@Library('jenkins-shared-lib')_

stage('Print Build Info') {
    gitCheckout()
} stage('Disable balancer') {
    buildApp()
} stage('Deploy') {
    deploy()
} stage('Check Status') {
    checkStatus()
}
